#include <bits/stdc++.h>
using namespace std;

void calc() {
    int n;
    cin >> n;
    vector<int> v(n);
    for (int i = 0; i < n; i++) {
        cin >> v[i];
    }
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        calc();
    }
    return 0;
}